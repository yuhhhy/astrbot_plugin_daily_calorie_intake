# 更新日志

## 1.4.0

### 新增
- 营养素拆解：记录时同时估算蛋白质/碳水/脂肪（克），`/热量 今日`、`/热量 查询` 显示小计与逐条数据，CSV 导出包含营养素列；旧记录无营养素数据时显示 `-`
- `/热量 导出`：把全部饮食记录导出为 CSV 文件（UTF-8 BOM，Excel 直接打开），需平台支持文件发送；发送失败自动降级为纯文本账单（最多展示最近 20 条）
- `/热量 查询 [日期]`：查询任意日期（昨天、前天或 YYYY-MM-DD）的摄入明细
- `/热量 撤销` 支持 `[日期]` 参数（缺省今天），可撤销之前日期的记录
- 文字记录支持补记最近 7 天：`record_daily_calorie_intake` 工具新增 `date` 参数（如“昨天晚饭忘了记”会记到昨天）
- `/热量 清空`：二次确认后一键清空个人档案与全部饮食记录，方便用户保护隐私或重新开始

### 重构
- 深度拆分：交互式会话流程（`interactive.py`）、LLM 工具逻辑（`tools.py`）、图片分析入账（`image_flow.py`）从 `main.py` 外移，入口瘦身为纯委托薄壳（1189 → 383 行）
- 每用户读写锁下沉到 `UserStateStore`，新增 `locked()` / `update()` 事务接口，所有写路径统一获得并发保护
- 报表/账单/CSV 渲染统一收拢到 `reply_ui.py`
- 全量代码通过 ruff format 与 ruff check（22 处自动修复：现代类型导入、注解去引号等）

## 1.3.2

### 变更
- 年龄输入范围由 18～100 放宽为 16～100 岁；16～17 岁用户建档或修改年龄时会附加专业建议提示

### 新增
- WebUI 配置新增「每用户最多保留的饮食记录条数」（默认 1000），超出后自动丢弃最旧记录

### 重构
- 代码结构重构：业务逻辑拆分到 `core/` 子包（constants / nutrition / llm_parsing / state_store / reply_ui / session_filter），`main.py` 瘦身为插件入口，纯函数可独立单元测试
- 记录条数裁剪逻辑收拢到 `UserStateStore.save()`，所有写路径统一生效

## 1.3.1

### 新增
- 提供 WebUI 插件配置界面（`_conf_schema.json`）：支持配置日期边界时区偏移（默认 UTC+8）、新用户默认记录开关、图片识别后 AI 个性化回复开关
- 关闭图片 AI 回复时，图片入账改回纯文本摘要，节省 token

## 1.3.0

### 新增
- 向 AI 注册 `record_daily_calorie_intake` 工具：用户直接用文字描述饮食（如"我午饭吃了一碗牛肉面，帮我记一下"），AI 估算热量后自动入账，与图片记录互补

### 变更
- 移除 `/热量 记录 <数字>` 手动记录指令，文字记录统一改为对话描述 + LLM 工具自动入账
- `/热量 今天` 指令更名为 `/热量 今日`
- 建档完成、`/自动记录` 开关与状态提示同步更新，覆盖文字与图片两种记录方式

### 修复
- 插件所有纯文本回复改为强制纯文本发送（`.use_markdown(False)`），修复 QQ 官方把 `#id` 渲染成标题、单个换行被合并成一行的问题
- 统一使用 UTC+8（Asia/Shanghai）作为“今天”的日期边界，修复服务器时区导致记录被划到错误日期的问题
- `/热量 今日` 明细改为 Markdown 表格输出，且不再显示记录 ID

## 1.2.1

### 修复
- 给 `list_daily_calorie_records` / `undo_daily_calorie_record` 两个 LLM 工具补默认参数，避免模型省略 `date`/`selector` 时抛出 `TypeError`
- 强化 `parse_food_analysis`：容忍模型返回的布尔/数值变体（如 `"true"`、`650.5`、`"650"`），避免误判或崩溃
- `_load_state` 对损坏的 KV 数据做归一化，避免类型异常导致崩溃
- 修复 `configure` 并发丢更新：保存前在锁内重载状态
- `record` / `auto_record` 的读-改-写补锁；`record` 不再跨 `yield` 持锁

## 1.2.0

- 使用当前会话的多模态模型分析食物图片并自动入账
- 入账后的回复由 AI 结合当前会话上下文、人设、用户档案与当日记录生成
- 新增 `/热量 配置` 局部修改档案，修改后自动重算 TDEE 与目标
- 新增 `/自动记录 开启|关闭|状态` 控制记录开关
- 向 AI 注册 `list_daily_calorie_records` 与 `undo_daily_calorie_record` 工具
